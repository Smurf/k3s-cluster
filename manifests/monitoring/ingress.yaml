---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ grafana_fqdn }}`)
      kind: Rule
      services:
        - name: kube-prometheus-stack-grafana
          port: 80
{% if traefik_cf_le %}
      middlewares:
        - name: redirect-https
          namespace: kube-system
  tls:
    certResolver: cf_le
{% endif %}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-insecure
  namespace: monitoring
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`{{ grafana_fqdn }}`)
      kind: Rule
      services:
        - name: kube-prometheus-stack-grafana
          port: 80
      middlewares:
        - name: redirect-https
          namespace: kube-system
